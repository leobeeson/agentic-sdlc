<!-- TEMPLATE: the domain glossary, the ubiquitous language of the project.
     Owner: the context-agent, the single writer of ai_docs/reference/. Path:
     ai_docs/reference/CONTEXT.md (the project spine). Every agent reads this
     file and uses these terms exactly; code, specs, and generated artefacts
     use this vocabulary. The arch-blueprint consultation seeds it; the
     context-agent keeps it current, refreshing on demand and when the
     reconciler flags vocabulary drift. Keep this file under 200 lines. -->

<!-- PROVENANCE (append-only; only created is fixed) -->
created:        <ISO-8601 timestamp>
modified:       [<ISO-8601 timestamp>]
commits:        [<short-sha>]
agents:         [context-agent]
run-ids:        [<run-id>]
back-refs:      [charter.md, prd.md, architecture.md, <the project code>]
forward-refs:   [read by every agent role]
<!-- END PROVENANCE -->

# Context: domain glossary

One entry per term. Each entry carries the precise meaning of the term in this
project, where the term is used, and the synonyms to avoid.

## Terms

### <Term>

<The precise meaning of the term in this project, and where it is used. One
short paragraph. Avoid: <the synonyms that must not be used in its place>.>
