# Implementation Plan: <project name>

Produced in phase 3 by the `plan-implementation` skill. Reads the architecture and the requirements document. This plan, together with the spec documents under `specs/` and the task registry `specs/index.md`, is the bridge into the implementation phase.

## Approach

The build strategy in a few sentences, including what the first deliverable is.

## Work breakdown

The areas of concern, each becoming a spec document `specs/NN-<area>.md`, and the tasks within each.

| Area | Spec document | Tasks |
|------|---------------|-------|

## Sequencing and dependencies

The order tasks must be built in, and the dependencies between them.

## First deliverable

The smallest coherent set of tasks that produces something working end to end.

## Testing strategy

How tasks will be verified: the levels of testing, and the conventions captured in `reference/testing-conventions.md`.

## Risks

| Risk | Affected tasks | Mitigation |
|------|----------------|------------|
