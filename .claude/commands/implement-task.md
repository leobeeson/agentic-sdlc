---
description: Phase 4. Run the implementation loop for one task.
argument-hint: <task-id>
---

Use the `implement-task` skill to run the implementation loop for task $ARGUMENTS: explore, prepare, implement, run the review panel, consolidate, walk through, and reconcile. Each phase is independently callable.
