---
description: Phase 3. Produce the spec documents and the task registry.
---

Use the `plan-implementation` skill to run phase 3: turn the architecture and requirements into spec documents grouped by area and a task registry, the join into the implementation loop.
